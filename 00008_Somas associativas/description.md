Mais uma vez, você receberá um array `$numeros` com muitos números e queremos obter a soma de todos os números.

O problema é que `$numeros` é um array associativo e um **for** não funciona.

Boa sorte!